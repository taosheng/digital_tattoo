# 如何在 Solana 區塊鏈上找回您的數位刺青 (自助指南)

由於您的數位刺青已被永久記錄在 Solana 區塊鏈上，您永遠無需擔心我們的伺服器關閉！您可以隨時隨地獨立找回並重建您的資料，完全不需要依賴我們的應用程式或後端系統。

## 1. 尋找區塊鏈紀錄
所有的數位刺青都以安全的方式，透過交易的「備忘錄 (Memo)」存放在 Solana 網路上的一個指定金庫錢包地址中。
1. 開啟任何公開的區塊鏈瀏覽器，例如 [Solana Explorer](https://explorer.solana.com) 或是 [Solscan](https://solscan.io)。
2. 輸入我們官方統一彙整的接收者錢包地址：`DHTjb119U6MdpHLHVfqn2bddgJWuWi4c3e84WjXzVBZF`。
3. 導航至 **Transactions (交易紀錄)** 分頁。
4. 檢查交易日誌，特別尋找標註有 **SPL Memo Program** 也就是備忘錄程式的指令內容。

## 2. 辨識您的資料
您上傳的每一個資料分片 (Chunk)，都明確標記了您的電子郵件信箱。
展開 Memo 日誌並利用搜尋功能找尋您的電子郵件。您將會發現以以下格式開頭的紀錄：
`TAO:您的信箱@gmail.com:<唯一_ID>|...`

緊接在 `|` 符號後面的字母代表了該刺青的類型：
- `|s|` 代表這是一個 **字串 (String) 刺青**
- `|f|` 代表這是一個 **檔案 (File) 刺青**

---

## 3. 重建您的字串刺青 (`s`)
字串刺青通常只需要 1 筆交易分片。格式如下所示：
`TAO:您的信箱@gmail.com:<唯一_ID>|s|<序號>|<總數>|<Base64_內容資料>`

**步驟：**
1. 找到您對應的日誌紀錄。
2. 複製文字的最後一段，也就是 `<Base64_內容資料>` 的部分。
3. 將這段複製的字串貼到網路上任何標準的 Base64 解碼工具（例如 `base64decode.org`）。
4. 解碼後的結果，即為您最初紀錄的純文字 UTF-8 訊息！

---

## 4. 重建您的檔案刺青 (`f`)
由於 Solana 的基礎單筆交易資料量有著嚴格的限制，較大的檔案會被「切割 (Sharding)」成好幾個連續的分片。檔案刺青的格式類似：
`TAO:您的信箱@gmail.com:<唯一_ID>|f|<序號>|<總數>|<Base64_內容資料>`

**步驟：**
1. **分片序號 0 (Metadata 檔案資訊)：** 尋找 `<序號>` 剛好為 `0` 的日誌 (`|f|0|`)。這個分片 **不包含** 您的檔案內容。相反地，解碼這段 Base64 會得到一個記載原始檔名與實際大小的 JSON 文字結構 (例如：`{"name": "photo.jpg", "size": 15000}`)。
2. **分片序號 1 至 總數 (檔案真實資料)：** 尋找所有擁有完全相同 `<唯一_ID>` 的日誌。它們的 `<序號>` 會從 `1` 一路排列到 `<總數>` 所標示的數字。
3. **字串拼接：** 請將序號 `1`, `2`, `3` ... 所有的 Base64 內容資料依序複製下來。將它們全部拼接在一起，形成「一整串」極長、毫無中斷的字串區塊 (請務必確保拼接過程中沒有留下任何空白或換行符號)。
4. **還原解碼：** 將這整包完整的 Base64 字串送入可以將 Base64 還原為檔案的解碼工具或腳本中。
5. 最後，使用在「分片序號 0」查到的原始檔案名稱與副檔名，將輸出的二進位結果儲存即大功告成！
